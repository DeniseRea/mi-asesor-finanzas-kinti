'use client';

import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { CheckCircle2, MessageCircle } from 'lucide-react'; import { useFinance } from '@/entities/finance/model/FinanceProvider'; import { AppCard } from '@/shared/components/AppCard';
export function CompleteProfile({locale}:{locale:'es'|'en'}){const {preferences,dispatch}=useFinance();const router=useRouter();const [phone,setPhone]=useState(preferences.phone);const [currency,setCurrency]=useState(preferences.currency);const done=()=>{dispatch({type:'updatePreferences',payload:{phone:phone.replace(/[^\d+ -]/g,'').slice(0,20),currency}});router.push(`/${locale}/dashboard`)};return <div className="mx-auto grid min-h-[calc(100dvh-10rem)] max-w-5xl place-items-center"><AppCard className="grid w-full overflow-hidden lg:grid-cols-[.7fr_1fr]"><div className="hidden bg-linear-to-br from-emerald-800 to-emerald-950 p-9 text-white lg:block"><MessageCircle size={35}/><h1 className="mt-7 text-3xl font-black">Termina de preparar tu Kinti</h1><p className="mt-4 leading-7 text-emerald-50/75">Configura tu moneda y deja listo el canal de WhatsApp para futuras integraciones.</p></div><div className="p-6 sm:p-9"><span className="grid size-12 place-items-center rounded-full bg-emerald-50 text-emerald-700"><CheckCircle2/></span><h1 className="mt-5 text-2xl font-black">Completa tu perfil</h1><p className="mt-2 text-sm text-slate-500">Este paso es opcional y podrás cambiarlo después.</p><div className="mt-7 space-y-4"><label className="block text-sm font-semibold">Teléfono / WhatsApp<input value={phone} onChange={(event)=>setPhone(event.target.value)} placeholder="+57 300 000 0000" className="mt-2 h-12 w-full rounded-xl border border-slate-200 px-4"/></label><label className="block text-sm font-semibold">Moneda<select value={currency} onChange={(event)=>setCurrency(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-slate-200 px-4"><option value="USD">USD — Dólar</option><option value="COP">COP — Peso colombiano</option><option value="EUR">EUR — Euro</option></select></label></div><div className="mt-7 flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><button onClick={()=>router.push(`/${locale}/dashboard`)} className="h-11 rounded-xl border border-slate-200 px-5 text-sm font-semibold">Omitir</button><button onClick={done} className="h-11 rounded-xl bg-emerald-800 px-5 text-sm font-bold text-white">Guardar y continuar</button></div></div></AppCard></div>}
